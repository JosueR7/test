CREATE DATABASE test1;
USE test;
CREATE TABLE IF NOT EXISTS areas (
    id int(11) NOT NULL AUTO_INCREMENT,
    nombre varchar(255),
    PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS empleados_rol (
    empleado_id int(11),
    rol_id int(11)
);

CREATE TABLE IF NOT EXISTS empleados (
    id int(11) NOT NULL AUTO_INCREMENT,
    nombre varchar(255),
    email varchar(255),
    sexo char(1),
    area_id int(11),
    boletin int(11),
    descripcion text,
    PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS roles (
    id int(11) NOT NULL AUTO_INCREMENT,
    nombre varchar(255),
    PRIMARY KEY (id)
);

INSERT INTO areas ( id, nombre) VALUES ( null, 'Calidad');
INSERT INTO areas ( id, nombre) VALUES ( null, 'Produccion');
INSERT INTO areas ( id, nombre) VALUES ( null, 'Ventas');

INSERT INTO roles ( id, nombre) VALUES ( null, 'Profesional de proyectos - Desarrollador');
INSERT INTO roles ( id, nombre) VALUES ( null, 'Gerente estrategico');
INSERT INTO roles ( id, nombre) VALUES ( null, 'Auxiliar administrativo');
